#include <iostream>
#include <map>
#include <string>

using namespace std;

map<char, char> mm = new map<char, char>();

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        string s_input;
        cin >> s_input;
        int left = 0;
        int right = s_input.length() - 1;
    }
}